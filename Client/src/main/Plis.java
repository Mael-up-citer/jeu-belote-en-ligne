package main;

//import src.main.Joueur;

public class Plis {/*
    private Paquet.Carte[] plis = new Paquet.Carte[4];
    private int valueBigest;    // Valeur de la carte la plus importante
    private Joueur owne;    //  Dit qui est maitre de ce plis a chaque tour 

    public int getValue() {
        int sum = 0;

        for(int i = 0; i < plis.length; i++)
            sum += plis[i].getNbPoint();

        return sum;
    }
        */
}