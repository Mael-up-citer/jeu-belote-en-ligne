package main;


import java.util.List;

public class Rules {

    public static List<Carte> playable() {
        // Regarde ce qui est demandé
        // Si atout
            // Si on a de l'atout on doit monter si on ne peut pas on met une plus petite
            // Sinon joueur ce qu'on veut
        // Sinon
            // Si on a la couleur on doit la jouer
            // sinon
                // Si on a pas la main mettre de l'atout sinon ce qu'on veut
        return null;
    }
}
